// Forma normal

const name = 'Gabriel';
const idade = 16;

const user = {
    name: name,
    idade: idade
};


/////////////////////////////////

const name1 = 'Gabriel';
const idade1 = 16;

const user1 = {
    name1,
    idade1
};

console.log(user1)
// return > { name1: 'Gabriel', idade1: 16 }
// Adiciona automaticamente ao array uma chave com o mesmo nome da variável que contém o valor

